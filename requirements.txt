# Core numerical/model dependencies
numpy>=2.4.6
scipy>=1.17.1
jax>=0.10.1
jaxlib>=0.10.1
optax>=0.2.8

# Rendering and GUI dependencies for app.py
glfw>=2.10.0
moderngl>=5.12.0
imgui-bundle>=1.92.801
pillow>=12.2.0
pyopengl>=3.1.10

# Mitsuba/material/render support
drjit>=1.3.1
mitsuba>=3.8.0

# Project validation and legacy tools
PyQt6>=6.9.0
vedo>=2026.6.1
vtk>=9.6.2

# Robot communication helpers
ur-rtde>=1.6.0

# Real gripper camera for UR5 Robot Mode. Optional: without it the mode still
# runs and falls back to clearly-marked synthetic frames, but no physical
# camera can be opened.
opencv-python>=4.10.0

# Test runner
pytest>=8.0.0


# mujoco and robot descriptions
mujoco==3.2.7
robot_descriptions>=1.12